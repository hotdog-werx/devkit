[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
  "pytest >= 9.0.2",
  "pytest-mock >= 3.14.1",
  "pytest-cov >= 6.2.1",
  "ty >= 0.0.16",
]
repolish = [
  "repolish >= 1.0.0"
]
