# generated by hotdog-werx/devkit — manual changes will be overwritten
name: Finalize release

on:
  pull_request:
    types:
      - closed

permissions:
  contents: write

jobs:
  publish:
    uses: hotdog-werx/devkit/.github/workflows/__releez_publish.yaml@topic/repolish
    with:
      releez-action-ref: 'hotdog-werx/releez@v1'
      build-command: mise run build
      publish-command: mise run publish
    secrets: inherit

## post-release jobs — add your custom jobs here
