# generated by hotdog-werx/devkit — manual changes will be overwritten
name: ci-checks

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

jobs:
  repo-checks:
    uses: hotdog-werx/devkit/.github/workflows/__workspace_repo-checks.yaml@master
    with:
      enable-docs: false

  python-checks:
    uses: hotdog-werx/devkit/.github/workflows/__python_python-checks.yaml@master


## post-check jobs — add your custom jobs here
