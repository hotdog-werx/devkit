# generated by hotdog-werx/devkit — manual changes will be overwritten
name: Validate release

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

jobs:
  validate-release:
    uses: hotdog-werx/devkit/.github/workflows/__releez_validate-release.yaml@topic/repolish
    with:
      use-self-action: false

## post-validate-release jobs — add your custom jobs here
