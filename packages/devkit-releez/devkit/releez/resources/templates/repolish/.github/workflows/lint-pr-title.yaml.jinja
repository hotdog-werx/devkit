# generated by hotdog-werx/devkit — manual changes will be overwritten
name: Lint PR

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

jobs:
  lint-pr-title:
    uses: hotdog-werx/devkit/.github/workflows/__releez_lint-pr-title.yaml@{{ releez_ref }}
    with:
      use-self-action: {{ use_self_action | lower }}

## repolish-start[additional-lint-pr-title-jobs]
## post-lint-pr-title jobs — add your custom jobs here
## repolish-end[additional-lint-pr-title-jobs]
