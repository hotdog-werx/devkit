# generated by hotdog-werx/devkit — manual changes will be overwritten
name: Finalize release

on:
  pull_request:
    types:
      - closed

permissions:
  contents: write

jobs:
  publish:
    uses: hotdog-werx/devkit/.github/workflows/__releez_publish.yaml@{{ releez_ref }}
    with:
      releez-action-ref: {% if use_self_action %}'./'{% else %}'hotdog-werx/releez@{{ releez_action_version }}'{% endif %}
      build-command: {{ build_command }}
      publish-command: {{ publish_command }}
    secrets: inherit

## repolish-start[additional-jobs]
## post-release jobs — add your custom jobs here
## repolish-end[additional-jobs]
