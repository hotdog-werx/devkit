# generated by hotdog-werx/devkit — manual changes will be overwritten
name: Validate release

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

jobs:
  validate-release:
    uses: hotdog-werx/devkit/.github/workflows/__releez_validate-release.yaml@{{ releez_ref }}
    with:
      use-self-action: {{ use_self_action | lower }}

## repolish-start[additional-validate-release-jobs]
## post-validate-release jobs — add your custom jobs here
## repolish-end[additional-validate-release-jobs]
